import '../styles/style.css';
import initializeWebsite from './website';
import UI from "./components/UI";

addEventListener("DOMContentLoaded", UI.loadPage());
// addEventListener("DOMContentLoaded", initializeWebsite);
